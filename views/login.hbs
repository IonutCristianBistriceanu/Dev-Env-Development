{{> header}}
<div class="container">
    <div class="row">
        <div class="col-lg-4">
            {{> loginform}}
        </div>

        <div class="col-xl-8">
        </div>
    </div>
</div>

{{> footer}}